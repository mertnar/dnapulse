{{- if .Values.services.processing.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mini-dna-platform.fullname" . }}-processing
  labels:
    {{- include "mini-dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: processing
spec:
  replicas: {{ .Values.services.processing.replicas }}
  selector:
    matchLabels:
      {{- include "mini-dna-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: processing
  template:
    metadata:
      labels:
        {{- include "mini-dna-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: processing
      {{- if .Values.services.processing.metrics.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.services.processing.metrics.port }}"
        prometheus.io/path: "{{ .Values.services.processing.metrics.path }}"
      {{- end }}
    spec:
      {{- with .Values.security.serviceAccount }}
      serviceAccountName: {{ include "mini-dna-platform.serviceAccountName" $ }}
      {{- end }}
      containers:
      - name: processing
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.services.processing.image.repository }}:{{ .Values.services.processing.image.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.services.processing.service.port }}
          name: http
        {{- if .Values.services.processing.metrics.enabled }}
        - containerPort: {{ .Values.services.processing.metrics.port }}
          name: metrics
        {{- end }}
        env:
        - name: CONFIG_URL
          value: "http://config:8084"
        {{- range $key, $value := .Values.services.processing.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        resources:
          {{- toYaml .Values.services.processing.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.processing.service.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.processing.service.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: processing
  labels:
    {{- include "mini-dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: processing
  {{- if .Values.services.processing.metrics.enabled }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.services.processing.metrics.port }}"
    prometheus.io/path: "{{ .Values.services.processing.metrics.path }}"
  {{- end }}
spec:
  type: {{ .Values.services.processing.service.type }}
  ports:
  - port: {{ .Values.services.processing.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  {{- if .Values.services.processing.metrics.enabled }}
  - port: {{ .Values.services.processing.metrics.port }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  {{- end }}
  selector:
    {{- include "mini-dna-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: processing
{{- end }}
