{{- if .Values.services.decision.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dna-platform.fullname" . }}-decision
  labels:
    {{- include "dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: decision
spec:
  replicas: {{ .Values.services.decision.replicas }}
  selector:
    matchLabels:
      {{- include "dna-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: decision
  template:
    metadata:
      labels:
        {{- include "dna-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: decision
      {{- if .Values.services.decision.metrics.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.services.decision.metrics.port }}"
        prometheus.io/path: "{{ .Values.services.decision.metrics.path }}"
      {{- end }}
    spec:
      {{- with .Values.security.serviceAccount }}
      serviceAccountName: {{ include "dna-platform.serviceAccountName" $ }}
      {{- end }}
      containers:
      - name: decision
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.services.decision.image.repository }}:{{ .Values.services.decision.image.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.services.decision.service.port }}
          name: http
        {{- if .Values.services.decision.metrics.enabled }}
        - containerPort: {{ .Values.services.decision.metrics.port }}
          name: metrics
        {{- end }}
        env:
        {{- range $key, $value := .Values.services.decision.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        resources:
          {{- toYaml .Values.services.decision.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.decision.service.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.decision.service.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: decision
  labels:
    {{- include "dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: decision
  {{- if .Values.services.decision.metrics.enabled }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.services.decision.metrics.port }}"
    prometheus.io/path: "{{ .Values.services.decision.metrics.path }}"
  {{- end }}
spec:
  type: {{ .Values.services.decision.service.type }}
  ports:
  - port: {{ .Values.services.decision.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  {{- if .Values.services.decision.metrics.enabled }}
  - port: {{ .Values.services.decision.metrics.port }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  {{- end }}
  selector:
    {{- include "dna-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: decision
{{- end }}
