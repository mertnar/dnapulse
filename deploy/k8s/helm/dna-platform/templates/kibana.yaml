{{- if .Values.infrastructure.kibana.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dna-platform.fullname" . }}-kibana
  labels:
    {{- include "dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: kibana
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "dna-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: kibana
  template:
    metadata:
      labels:
        {{- include "dna-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: kibana
    spec:
      containers:
      - name: kibana
        image: "{{ .Values.infrastructure.kibana.image.repository }}:{{ .Values.infrastructure.kibana.image.tag }}"
        imagePullPolicy: {{ .Values.infrastructure.kibana.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.infrastructure.kibana.service.port }}
          name: http
        env:
        - name: server.name
          value: {{ .Values.infrastructure.kibana.config.server.name | quote }}
        - name: server.host
          value: {{ .Values.infrastructure.kibana.config.server.host | quote }}
        - name: elasticsearch.hosts
          value: {{ .Values.infrastructure.kibana.config.elasticsearch.hosts | quote }}
        resources:
          {{- toYaml .Values.infrastructure.kibana.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /api/status
            port: {{ .Values.infrastructure.kibana.service.port }}
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/status
            port: {{ .Values.infrastructure.kibana.service.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: kibana
  labels:
    {{- include "dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: kibana
spec:
  type: {{ .Values.infrastructure.kibana.service.type }}
  ports:
  - port: {{ .Values.infrastructure.kibana.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "dna-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: kibana
{{- end }}
