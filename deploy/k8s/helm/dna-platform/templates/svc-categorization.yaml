{{- if .Values.services.categorization.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dna-platform.fullname" . }}-categorization
  labels:
    {{- include "dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: categorization
spec:
  type: {{ .Values.services.categorization.service.type }}
  ports:
    - port: {{ .Values.services.categorization.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "dna-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: categorization
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dna-platform.fullname" . }}-categorization
  labels:
    {{- include "dna-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: categorization
spec:
  replicas: {{ .Values.services.categorization.replicaCount }}
  selector:
    matchLabels:
      {{- include "dna-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: categorization
  template:
    metadata:
      labels:
        {{- include "dna-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: categorization
    spec:
      containers:
        - name: categorization
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.services.categorization.image.repository }}:{{ .Values.services.categorization.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: HTTP_PORT
              value: "8080"
            - name: CONFIG_URL
              value: "http://config:8084"
            - name: CONFIG_SCOPE
              value: "categorization"
            - name: CONFIG_SSE_URL
              value: "http://config:8084"
            - name: BUS_BROKER
              value: "{{ include "dna-platform.fullname" . }}-redpanda:9092"
          resources:
            {{- toYaml .Values.services.categorization.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
{{- end }}
